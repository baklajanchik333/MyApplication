<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookApp">
        <activity android:name=".Activities.Dashboard.DashboardAdminActivity" />
        <activity android:name=".Activities.Dashboard.DashboardUserActivity" />
        <activity android:name=".Activities.Auth.LoginActivity" />
        <activity android:name=".Activities.Auth.RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Activities.SplashActivity"
            android:theme="@style/FullScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>